package entites;

import entites.enums.TeamColor;
import javafx.scene.image.ImageView;

public class Tower extends Piece {

    public Tower(TeamColor teamColor, PositionInterface position, ImageView imageView) {
        super(teamColor, position, imageView);
    }
}
