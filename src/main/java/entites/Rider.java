package entites;

import entites.enums.TeamColor;
import javafx.scene.image.ImageView;

public class Rider extends Piece {

    public Rider(TeamColor teamColor, PositionInterface position, ImageView imageView) {
        super(teamColor, position, imageView);
    }
}
