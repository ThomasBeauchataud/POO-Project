package entites;

import entites.enums.TeamColor;
import javafx.scene.image.ImageView;

public class Queen extends Piece {

    public Queen(TeamColor teamColor, PositionInterface position, ImageView imageView) {
        super(teamColor, position, imageView);
    }
}
