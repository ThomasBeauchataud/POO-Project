package entites;

import entites.enums.TeamColor;
import javafx.scene.image.ImageView;

public class King extends Piece {

    public King(TeamColor teamColor, PositionInterface position, ImageView imageView) {
        super(teamColor, position, imageView);
    }
}
